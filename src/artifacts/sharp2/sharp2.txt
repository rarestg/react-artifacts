## What We Built: A Design System Showcase

This artifact is a **living component library** — a Sharp2 artifact folder in this repo that renders shared primitives
and sharp2-local examples in a way you can see, click, and test. It serves the same purpose as tools like Figma
(design) or Storybook (development), but with repo-backed React components and the shared artifact theme.

### What Figma and Storybook Do (and how this compares)

**Figma** is a design tool where designers create static mockups of interfaces. The problem: designs are pictures, not code. Developers have to interpret them, and drift happens.

**Storybook** is a developer tool that renders React components in isolation so you can see all their states (hover, active, disabled, error, etc.) without building the full app. The problem: it requires setup, config files, and a build process.

**This artifact** combines both ideas:
- It's visual like Figma — you can see exactly how things look
- It's interactive like Storybook — you can click buttons, toggle states, type in inputs
- It's implementation-backed — import shared primitives from `src/components` and keep them inside `ArtifactThemeRoot`
- It's repo-native — it runs in this artifact viewer and is portable to React apps that also bring the shared components and artifact theme

---

## The Sharp UI Visual Style Guide

The design system follows a strict set of constraints we call **Sharp UI**. These rules create a dense, scannable, terminal-adjacent aesthetic that prioritizes information density over decoration.

### The Core Rules

| Rule | What it means | Why |
|------|---------------|-----|
| **Zero border-radius** | No `rounded-*` classes anywhere | Sharp edges = professional, dense, no wasted pixels |
| **Zero shadows** | No `shadow-*` for hierarchy | Use borders instead — shadows are fuzzy and imprecise |
| **Zero alpha backgrounds** | No `bg-white/80` or transparency | Solid colors only — alpha causes layering issues |
| **Border-driven hierarchy** | Distinguish elements with borders, not shadows or blur | Crisp, predictable, works at any zoom level |
| **Focus-visible rings** | `focus-visible:ring-2` + offset | Rings are allowed for keyboard focus; keep them high-contrast and unclipped |
| **Monospace-forward** | Message content uses `font-mono` | Terminal-adjacent feel, consistent character widths |

### What's Explicitly Forbidden

```jsx
// ❌ NEVER do this in Sharp UI
className="rounded-lg shadow-md bg-white/90 backdrop-blur"

// ✅ Do this instead
className="border border-[var(--border)] bg-[var(--surface)]"
```

---

## The Component Inventory

Sharp2 now separates reusable shared primitives from local showcase and domain examples. Import shared primitives from
`src/components`; keep sharp2-local examples in this artifact until their semantics are proven as shared APIs.

### Shared Primitives (`src/components`)

These primitives are shared application APIs. Render them under `ArtifactThemeRoot` so their tokens, focus rings, and
theme-boundary guards resolve correctly.

| Component | Purpose | Key Props |
|-----------|---------|-----------|
| `Button` | Shared action primitive | `variant`: `default`, `primary`, `ghost`, `danger`; `size`: `sm`, `md`, `lg` |
| `Input` | Shared text-entry primitive | `error` for validation state |
| `Tag` | Shared inline label/badge primitive | `variant`: `base`, `muted`, `solid` |
| `Panel` | Shared structural container primitive | `variant`: `default`, `muted`, `dashed` |
| `Checkbox` | Shared boolean selection primitive | `checked`, `onCheckedChange`, `label`, `reserveLabel` |
| `Toggle` | Shared on/off primitive | `checked`, `onCheckedChange`, `label`, `reserveLabel` |
| `CopyButton` | Shared copy-to-clipboard primitive | `text` |
| `CopyableLabel` | Shared metadata-with-copy primitive | `value`, `icon` |
| `StatusTag` | Shared stable-width status primitive | `label`, `active`, `reserveLabel`, `icon` |

### Sharp2-Local Showcase Examples

These examples live under `src/artifacts/sharp2/components`. Use them as reference implementations for this showcase,
not as shared APIs.

| Component | Purpose | Key Props |
|-----------|---------|-----------|
| `SearchInput` | Sharp2-local managed combobox example | `value`, `onChange`, `results`, `onSelect` |
| `Popover` | Sharp2-local dropdown example anchored to a trigger | `open`, `onToggle`, `trigger` |
| `Row` | Sharp2-local clickable list item example | `selected`, `onClick` |
| `CodeBlock` | Sharp2-local literal code block with copy support | `language`, `children` |
| `Section` | Sharp2-local page section example | `title`, `cols` |
| `SubSection` | Sharp2-local labeled group example | `label` |

### Sharp2-Local Conversation Domain Examples

These examples live under `src/artifacts/sharp2/conversation`. They may import shared primitives internally, but the
conversation semantics remain local to sharp2.

| Component | Purpose | Key Props |
|-----------|---------|-----------|
| `MessageCard` | Sharp2-local single message renderer | `role`, `content`, `renderMode`, `onToggleRender` |
| `ToolCall` | Sharp2-local tool invocation renderer | `tool`, `input`, `output`, `status` |
| `ConversationTurn` | Sharp2-local turn grouping renderer | `turnNumber`, `items`, `visibleTypes` |
| `MessageTypeToggle` | Sharp2-local message type filter control | `label`, `count`, `checked`, `color` |
| `TokenCounter` | Sharp2-local context window usage meter | `used`, `limit`, `label` |

---

## Key Patterns to Understand

### 1. The Reserve Label Pattern (Stable Width)

When a label changes (like "Copy" → "Copied"), the button width can jump. This is jarring. The **reserve label** pattern fixes it:

```jsx
<span className="relative inline-grid">
  {/* Hidden text reserves the width */}
  <span className="col-start-1 row-start-1 opacity-0 pointer-events-none">
    {reserveLabel}
  </span>
  {/* Visible text renders on top */}
  <span className="col-start-1 row-start-1">
    {currentLabel}
  </span>
</span>
```

Used in: `StatusTag`, `CopyButton`, `CopyableLabel`, `Checkbox`, `Toggle`

### 2. The Clickability Contract

Every clickable element must have ALL THREE:

1. **`cursor-pointer`** — pointer cursor on hover
2. **Visible hover state** — background, border, or text change
3. **Active/pressed state** — darker feedback when clicked

```jsx
// ✅ Correct
className="cursor-pointer hover:bg-[var(--surface-muted)] active:bg-[var(--surface-pressed)]"

// ❌ Missing active state
className="cursor-pointer hover:bg-[var(--surface-muted)]"
```

### 3. Focus States

All interactive elements use focus-visible rings with a small offset for keyboard clarity:

```jsx
className="focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--ring)] focus-visible:ring-offset-1 focus-visible:ring-offset-[color:var(--surface)]"
```

The `focus-visible` variant means the ring only appears for keyboard navigation, not mouse clicks.
Prefer a high-contrast ring; avoid dim-only focus on filled/dark buttons.
Use the ring token (`--ring`) across controls to keep focus cues consistent.
Use one primary focus cue per component: rows use a left bar + background; controls use a ring.

### 4. Message Rendering Modes

Messages have two render modes:
- **Literal**: `whitespace-pre-wrap`, preserves exact input (default for User)
- **Rendered**: Parses markdown into headings, lists, code blocks (default for Assistant)

The toggle button shows "Raw" when in literal mode, "Rendered" when in rendered mode.

---

## How to Use This as You Build

### Workflow 1: Import Shared Primitives

When you need a shared Sharp UI primitive in an artifact:

1. Wrap the artifact root with `ArtifactThemeRoot`.
2. Import the primitive from `src/components`.
3. Keep the primitive under the artifact theme boundary so tokens, focus rings, and shared component guards work.

Example:

```tsx
import { ArtifactThemeRoot } from '../../components/ArtifactThemeRoot';
import { Button } from '../../components/Button';

export default function Artifact() {
  return (
    <ArtifactThemeRoot className="min-h-screen bg-[var(--surface-muted)] text-[var(--text)]">
      <Button variant="primary">Save</Button>
    </ArtifactThemeRoot>
  );
}
```

### Workflow 2: Reference for Styling Decisions

When you're styling something custom:

1. Check the design tokens at the top of the file
2. Use the shared token palette (`var(--surface)`, `var(--text)`, `var(--border)`, etc.)
3. Follow the same border/spacing patterns
4. Apply the clickability contract

### Workflow 3: Test States Before Building

Before implementing a feature:

1. Find a similar component in the showcase
2. Click through its states (hover, active, focus, disabled)
3. Understand the expected behavior
4. Then build your version

---

## The Design Token Scale

These are the foundational values everything is built from:

```js
// Surfaces (backgrounds)
surface.0 = 'var(--surface)'          // panels, cards
surface.1 = 'var(--surface-muted)'    // muted sections, hover states
surface.2 = 'var(--surface-strong)'   // strong hover / grouping
surface.3 = 'var(--surface-pressed)'  // pressed / active states

// Borders
border.default = 'var(--border)'        // standard borders + dividers
border.strong  = 'var(--border-strong)' // emphasis

// Text
text.0 = 'var(--text)'         // primary content
text.1 = 'var(--text-muted)'   // secondary content
text.2 = 'var(--text-subtle)'  // muted, timestamps, hints
```

### Semantic Status Tokens

| Token | Meaning | Used For |
|-------|---------|----------|
| `--success` / `--success-weak` | Success | Success states, confirmations |
| `--warning` / `--warning-weak` | Warning | Warnings, caution states |
| `--danger` / `--danger-weak` | Error/Danger | Error states, destructive actions |
| `--info` / `--info-weak` | Info | Informational accents |

### Categorical Palette (Message Type Colors)

These are categorical (not status) colors for classifying message types and tags.

| Token | Meaning | Used For |
|-------|---------|----------|
| `--category-blue` / `--category-blue-weak` | User | Left bars, dots, filter chips |
| `--category-green` / `--category-green-weak` | Assistant | Left bars, dots, filter chips |
| `--category-amber` / `--category-amber-weak` | Thinking | Left bars, dots, filter chips |
| `--category-violet` / `--category-violet-weak` | Tool | Left bars, dots, filter chips |
| `--category-red` / `--category-red-weak` | Critical | Special highlights if needed |
| `--category-cyan` / `--category-cyan-weak` | System | Left bars, dots, filter chips |
| `--category-pink` / `--category-pink-weak` | Note | Left bars, dots, filter chips |
| `--category-lime` / `--category-lime-weak` | Marker | Left bars, dots, filter chips |

Guidelines:
- Use `--category-*-weak` for selected row backgrounds or filter chips.
- Use `--category-*` for dots, left bars, or badges.
- Keep text labels in `--text` or `--text-muted` so color is not the only cue.

---

## Quick Reference: Sharp UI Checklist

Before shipping any component, verify:

- [ ] No `rounded-*` classes
- [ ] No `shadow-*` classes
- [ ] No alpha backgrounds (`bg-*/80`, `bg-white/50`)
- [ ] Borders used for hierarchy, not shadows
- [ ] `cursor-pointer` on all clickables
- [ ] Hover state visible (bg or border change)
- [ ] Active state visible (darker than hover)
- [ ] Focus uses `focus-visible:ring-2` + `ring-offset-1` (high-contrast)
- [ ] Dynamic labels use reserve label pattern
- [ ] Monospace font for data/code content

---

This artifact is your source of truth. When in doubt, open it, find a similar component, and match its patterns. The goal is visual consistency across your entire app — every button should feel like it belongs with every other button.
