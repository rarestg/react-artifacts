export const panelHeaderRowClass =
  'flex flex-wrap items-center justify-between gap-4 border-b border-[var(--border)] px-4 py-3';

export const panelHeaderTextClass = 'flex min-w-0 flex-col gap-1';

export const panelHeaderTitleClass = 'text-xs font-semibold uppercase tracking-[0.3em] text-[var(--text-muted)]';

export const panelHeaderMetaClass = 'flex flex-wrap items-center gap-2 text-[11px] font-mono text-[var(--text-muted)]';

export const panelHeaderActionsClass = 'flex flex-wrap items-center gap-2 min-w-0 basis-full sm:basis-auto';
